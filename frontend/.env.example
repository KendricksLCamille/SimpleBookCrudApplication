# Copy this file to `.env` in the same directory and adjust as needed.
# The frontend (Vite) reads variables prefixed with VITE_ at build time.
# This URL should point to your running backend API base URL.
# Examples:
# - Local HTTPS dev certificate (example): VITE_API_URL=https://localhost:7089
# - Local HTTP: VITE_API_URL=http://localhost:5152
# - Custom: VITE_API_URL=http://127.0.0.1:5089

VITE_API_URL=
